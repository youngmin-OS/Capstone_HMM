spring.application.name=api
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/deepfake}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:deepfake}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:deepfake}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.h2.console.enabled=false
ai.analyze.url=${ANALYZE_URL:http://localhost:8000}
ai.protect.url=${PROTECT_URL:https://kmlvwtyfyf55b9-8000.proxy.runpod.net}
app.base-url=${APP_BASE_URL:http://localhost:8080}
jwt.secret=${JWT_SECRET:dev-only-change-this-secret-key-32b}
jwt.expiration-ms=${JWT_EXPIRATION_MS:3600000}
